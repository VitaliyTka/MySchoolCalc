@inherits LayoutComponentBase

<RadzenDialog />
<RadzenNotification />
<RadzenContextMenu />
<RadzenTooltip />

<PageTitle>MySchoolCalc</PageTitle>

<RadzenLayout class="rz-shadow-3">
    <RadzenHeader>
        <div class="d-flex align-items-center">
            <RadzenSidebarToggle Click="@(() => sidebarExpanded = !sidebarExpanded)" />
            <RadzenLabel Text="MySchoolCalc" />
        </div>
    </RadzenHeader>
    <RadzenSidebar @bind-Expanded="@sidebarExpanded">
        <RadzenPanelMenu>
            @*<RadzenPanelMenuItem Text="Home" Icon="home" Path="/" />*@
            <RadzenPanelMenuItem Text="Math Formulas" Icon="account_box" Path="MathFormulas" />
            <RadzenPanelMenuItem Text="Geometric Formulas" Icon="account_box" Path="GeometricFormulas" />
        </RadzenPanelMenu>
    </RadzenSidebar>
    <RadzenBody>
        <div class="p-2">
            @Body
        </div>
    </RadzenBody>
</RadzenLayout>

@code {
    bool sidebarExpanded = true;
}